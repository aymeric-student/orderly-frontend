// import { expect, test } from '@playwright/test';
//
// const url = 'http://localhost:4200/';
//
// test('Navbar should display brand and menu link', async ({ page }) => {
//     await page.goto(url);
//
//     const navbar = await page.locator('mat-toolbar.navbar');
//     await expect(navbar).toBeVisible();
//
//     const brandLink = await page.locator('a.brand');
//     await expect(brandLink).toBeVisible();
//
//     const menuLink = await page.locator('a[routerlink="/menu"]');
//     await expect(menuLink).toBeVisible();
// });
